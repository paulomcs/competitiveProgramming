#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {

    int cicles, mAmnt, gAmnt;
    scanf("%d", &cicles);

    for(int i = 0; i < cicles; i++){
        scanf("%d %d", &mAmnt, &gAmnt);
        char Arr[mAmnt];
        scanf("%s", m);
    }

    return 0;
}